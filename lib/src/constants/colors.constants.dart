import 'package:flutter/material.dart';

const secondary = Color(0xff7B7FDA);
const warning = Colors.amber;
const accent = Color(0xffEA7052);
const error = Colors.redAccent;
const info = Colors.cyan;
const light = Colors.white;
const dark = Colors.black;
const success = Colors.green;

Color? get complimentry => Colors.white;

Color? get complimentry20 => Colors.grey[100];

Color? get complimentry40 => Colors.grey[200];

Color? get complimentry60 => Colors.grey[300];

Color? get complimentry80 => Colors.grey[400];
